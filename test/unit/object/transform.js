import { describe, test } from "vitest";

describe('Transform', () => {
    describe('construction & conversion', () => {
        test.todo.for([])('constructor ($constructor) creates from array with correct values', ({ values, rows, cols, constructor }) => { })
        test.todo.for([])('toArray returns correct values ($values)', ({ values, rows, cols, constructor }) => { })
        test.todo.for([])('clone keeps correct type ($constructor)', ({ values, rows, cols, constructor }) => { })
        test.todo.for([])('clone keeps correct values ($values) ($constructor)', ({ values, rows, cols, constructor }) => { })
        test.todo.for([])('($values) toJSON returns correct json', ({ values, rows, cols, constructor }) => { })
        test.todo.for([])('($values) fromJSON creates correct matrix', ({ values, rows, cols, constructor }) => { })
        test.todo.for([])('constructor ($constructor) creates from matrix with correct values', ({ matrix, constructor }) => { })
        test.todo.for([])('constructor ($constructor) throws with incorrect arguments ($args)', ({ args, constructor }) => { })
    })
})
