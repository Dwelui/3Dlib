# Math

# Additions
- add getAt and setAt for Vector and Matrix classes for safe access when needed. `TypedArray`s do not throw errors when trying to access out of bound memory, only returns undefined or does not set anything.
